<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:gravity="center"
    >

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GAME OVER"
        android:paddingBottom="@android:dimen/app_icon_size"
        android:textColor="#93B5E5"
        android:textSize="25sp"
        android:textStyle="bold"
        android:fontFamily="serif-monospace"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/socialDistancingText"
        />

    <Button
        android:id="@+id/resetButton"
        android:layout_width="110dp"
        android:layout_height="120dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/facemask1" />

    <TextView
        android:id="@+id/socialDistancingText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="You didn't keep your distance"
        android:paddingTop="@android:dimen/app_icon_size"
        android:textColor="#93B5E5"
        android:textSize="15sp"
        android:textStyle="bold"
        android:fontFamily="serif-monospace"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/resetButton"
        />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="tap the player to restart"
        android:paddingTop="@android:dimen/app_icon_size"
        android:textColor="#93B5E5"
        android:textSize="15sp"
        android:textStyle="bold"
        android:fontFamily="serif-monospace"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/resetButton"
        />

</LinearLayout>